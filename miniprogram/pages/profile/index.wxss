.profile-card { display: flex; flex-direction: column; align-items: center; margin-top: 40rpx; }
.profile-logo { position: relative; overflow: hidden; width: 280rpx; height: 170rpx; background: #fff; }
.profile-logo image { position: absolute; top: -55rpx; width: 280rpx; }
.name { margin: 24rpx 0 8rpx; font-size: 36rpx; font-weight: 700; }
.button-primary { width: 100%; }
